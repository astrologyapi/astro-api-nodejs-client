/**
 * SDK version — single source of truth.
 * This value is embedded in the User-Agent header sent with every request.
 * Update this when cutting a new release.
 */
export const SDK_VERSION = "2.0.0";
